#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *x = malloc(3 * sizeof(int));
    if(x == NULL){
        return 1;
    }
    
    x[0] = 72;
    x[1] = 50;
    x[2] = 43;

    free(x);

}
